import { ReservationRegistrationForm } from './reservation-registration-form';

describe('ReservationRegistrationForm', () => {
  it('should create an instance', () => {
    expect(new ReservationRegistrationForm()).toBeTruthy();
  });
});
