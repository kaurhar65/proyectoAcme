import { ReservationExtendedDTO } from './reservation-extended-dto';

describe('ReservationExtendedDTO', () => {
  it('should create an instance', () => {
    expect(new ReservationExtendedDTO()).toBeTruthy();
  });
});
